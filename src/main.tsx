import React from "react";
import ReactDOM from "react-dom/client";
import { BlurBoi } from "./BlurBoi.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BlurBoi />
  </React.StrictMode>
);
